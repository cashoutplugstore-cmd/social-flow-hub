import{createFileRoute}from'@tanstack/react-router';import{useEffect,useState}from'react';import{Loader2}from'lucide-react';import{PageHeader,PageShell}from'@/components/page-shell';import{supabase}from'@/integrations/supabase/client';export const Route=createFileRoute('/admin/providers')({component:AdminProviders});function AdminProviders(){const[r,setR]=useState<any[]>([]);const[l,setL]=useState(true);useEffect(()=>{(async()=>{const{data}=await supabase.from('providers').select('*').order('created_at',{ascending:false});setR(data??[]);setL(false)})()},[]);return <PageShell><PageHeader eyebrow='الإدارة' title='المزودون' description='عرض مزودي الخدمات وحالتهم.'/><div className='mx-auto max-w-6xl px-4 py-8'>{l?<Loader2 className='mx-auto animate-spin'/>:<div className='space-y-3'>{r.map(x=><div key={x.id} className='surface-card flex flex-wrap justify-between gap-3 rounded-2xl p-5'><b>{x.name}</b><span>{x.is_active?'نشط':'متوقف'}</span></div>)}</div>}</div></PageShell>}